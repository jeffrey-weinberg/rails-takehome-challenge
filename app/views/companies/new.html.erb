<%= link_to 'Back', root_path, class: 'btn btn-primary mb-4' %>

<div class="card">
  <div class="card-header">
    <h2>New Company</h2>
  </div>
  <div class="card-body">
    <%= render 'form', company: @company %>
  </div>
</div>
