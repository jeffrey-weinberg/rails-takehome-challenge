<%= link_to 'Back', company_path(params[:company_id]), class: 'btn btn-primary mb-4' %>

<div class="card">
  <div class="card-header">
    <h2>New employee</h2>
  </div>
  <div class="card-body">
    <%= render 'form', employee: @employee %>
  </div>
</div>

